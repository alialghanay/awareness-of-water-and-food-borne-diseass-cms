/**
 * transmission-of-infection controller
 */

import { factories } from '@strapi/strapi'

export default factories.createCoreController('api::transmission-of-infection.transmission-of-infection');
